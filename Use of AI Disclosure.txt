AI was used somewhat in this project. Below is an extensive disclosure of all uses of AI.

1.
After Kosaraju's algorithm was implemented, the following prompt was ran through Github Copilot:
"Can you look through the program and look for the correctness of what has been implemented so far. Currently we should be able to read a matrix in CSR, store it in CSR, transpose it, reorder them in SCCs such that theoretically the number of feedback loops is globally minimized."
This used GPT-5.3-Codex • 0.9x to analyze the Code

It then responded with:
---------------------------------------------------------------------------------------------------------------------
I'll perform a careful review of the code for correctness in reading CSR format, storing it, transposing, and reordering by SCCs. 

Created 4 todos

Let me create a detailed code review analysis: 

Created memory file [](file:///c%3A/Users/jared/AppData/Roaming/Code/User/workspaceStorage/0f845c85e2dd3a757b6e368a56d523d9/GitHub.copilot-chat/memory-tool/memories/YWE2ZDkyM2ItMTJiOC00MTJiLWExZTYtYjVmYTNiNjFmMzU4/code_review.md)

Completed: *Review readFile() CSR parsing logic* (1/4)

## Code Review Summary

I've completed a thorough review of your DSM implementation. Here are the key findings:

### **Critical Issues Found** ⚠️

**1. Empty Final Rows Not Handled Correctly** (HIGH severity)
In both `readFile()` and `transpose()`, if the last matrix element is not in the final row, the remaining rows don't get initialized properly:
```java
for (; j<element[0]; j++)
    sm.rowPointer[j] = i;  // Stops at last element's row
// Missing: should fill remaining rows
sm.rowPointer[row] = ele;  // Only sets the very last entry
```
If elements end at row 500 but matrix has 600 rows, then `rowPointer[501..599]` remain as zeros instead of pointing to the total element count. This violates CSR format where empty rows at the end must have `rowPointer[i] = total_elements`.

**Fix**: After the loop, add this:
```java
for (; j <= row; j++)
    sm.rowPointer[j] = ele;
```

**2. No Bounds Validation on MTX Data** (MEDIUM severity)
The code doesn't validate that row/column indices in the MTX file are within [1, N]. Invalid indices could cause array out-of-bounds errors during the CSR construction loop.

**3. Unsorted Columns Within Rows** (LOW severity)  
Elements are sorted by row only, not by column within each row. Standard CSR requires `columnIndex` to be sorted within each row. This doesn't break your DFS/SCC algorithms (they're order-independent), but it deviates from CSR conventions.

---

### **What's Correct** ✓

**transpose()** algorithm is sound:
- Correctly converts CSR → expanded storage → transposed CSR
- Element counting and position calculation for transpose is correct
- The double index conversion (1-indexed ↔ 0-indexed) works algebraically

**reduceFeedback()** Kosaraju's algorithm is correct:
- First DFS produces correct topological order
- Second DFS on transposed matrix correctly identifies SCCs
- Matrix reordering using `indexMap` is correct
- Handles isolated nodes (degree 0) properly

---

### **Recommendations**

1. **Fix the empty rows bug immediately**—it will cause issues when CA.mtx has trailing empty rows
2. Add input validation for MTX row/column bounds
3. Optionally: Sort by (row, column) instead of just row for proper CSR format
4. **Test with a matrix that has trailing empty rows** to verify the fix

Would you like me to implement these fixes?
---------------------------------------------------------------------------------------------------------------------
This was outputted by Claude Haiku 4.5 • 0.3x




2. Generating test cases
Model used: Github Copilot using GPT-5.3-Codex • 0.9x
Prompt:
"Create a new file Test.java. As well as create some new test cases in the TestMatrices package. Go through all of these matrices that you are creating and test to ensure that 1. The resultant graph is isomorphic to the graph that was passed in. 2. The resultant matrix has successfully been ordered such that the number of feed back loops are minimized. These should test edge cases and should have descriptions before each test saying what exactly is being tested."
---------------------------------------------------------------------------------------------------------------------------
I’m going to add a dedicated test runner and several small edge-case `.mtx` files, then implement exhaustive checks for graph isomorphism and globally minimal feedback loops on those cases.

Read [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/TestMatrices)

Read [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/TestMatrices/Small.mtx)

I confirmed the matrix file format in your repo, so I’m now creating a new `DSM/Test.java` runner plus multiple small edge-case `.mtx` files under TestMatrices and wiring exhaustive verification into the runner.

Created [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/TestMatrices/Empty4.mtx)

Created [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/TestMatrices/Chain4.mtx)

Created [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/TestMatrices/Cycle3.mtx)

Created [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/TestMatrices/TwoSCC4.mtx)

Searched for files matching `**/DSM/Test.java`, no matches

Created [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/TestMatrices/SparseTrailingRows6.mtx)

Created [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/TestMatrices/SelfLoops5.mtx)

Created [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/DSM/Test.java)

I’ve created the new test runner and six edge-case matrices; next I’m compiling and executing `DSM.Test` to validate both isomorphism and global feedback-loop minimization checks end-to-end.

Ran terminal command: javac DSM/*.java; java DSM.Test
Completed with input: javac DSM/*.java; java DSM.Test

One test exposed a real issue in the current optimization logic (`Chain4` is not isomorphic after optimize), so I’m updating the runner to continue through all cases and report a full pass/fail summary instead of stopping at first failure.

Generating patch (29 lines) in [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/DSM/Test.java)


```

```


Generating patch (39 lines) in [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/DSM/Test.java)


```

```


Ran terminal command: javac DSM/*.java; java DSM.Test
Completed with input: javac DSM/*.java; java DSM.Test

I now have concrete failing behavior in multiple edge cases; I’m making one small harness update to capture runtime exceptions too, so all tests report outcomes in one run.

Generating patch (18 lines) in [](file:///c%3A/Users/jared/Projects/CPSC%20482/CPSC482-DSM/DSM/Test.java)


```

```


Ran terminal command: javac DSM/*.java; java DSM.Test
Completed with input: javac DSM/*.java; java DSM.Test

Implemented.

Created new test runner:
- Test.java

Created new edge-case matrix files:
- Empty4.mtx
- Chain4.mtx
- Cycle3.mtx
- TwoSCC4.mtx
- SparseTrailingRows6.mtx
- SelfLoops5.mtx

What the tests do:
1. Print a description before each test case.
2. Load the original matrix and an optimized copy.
3. Verify graph isomorphism by exhaustive permutation search with pruning.
4. Compute globally minimal feedback loops by brute force over all permutations.
5. Compare optimized feedback-loop count against the global minimum.

Execution result from running the suite:
1. Empty Matrix: PASS
2. Simple DAG Chain: FAIL (not isomorphic after optimize)
3. Single SCC Cycle: PASS
4. Two SCCs With Bridge: FAIL (not globally minimal feedback loops)
5. Trailing Empty Rows: FAIL (runtime exception)
6. Self Loops And Sparse Edges: FAIL (not isomorphic after optimize)

Summary:
- 2 passed, 4 failed, 6 total

Important implementation issue exposed:
- Runtime failure occurs in SparseMatrix.java during reduceFeedback on trailing-empty-row case.

If you want, next step is I can fix the reduceFeedback reordering logic so this suite passes.

--------------------------------------------------------------------------------------------------------------------------------------------------------------------

